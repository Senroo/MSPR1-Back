{% extends 'base.html.twig' %}
{% block title %}Modification partenaire - Back-office{% endblock %}
{% block body %}
    {{ include('sidebar/top.html.twig', {listItem: 8}) }}
    <style>

    </style>
    {{ include('page_title.html.twig', {title: "Modification d'un partenaire"}) }}
    <div class="main px-5 mt-5">
        {{ form_start(partnerEditForm) }}
        {{ form_row(partnerEditForm.name) }}
        {{ form_row(partnerEditForm.description) }}
        {{ form_row(partnerEditForm.logo, {
            label: 'Image à modifier'
        }) }}
        <button type="submit" class="btn btn-success float-right">Valider</button>
        {{ form_end(partnerEditForm) }}
    </div>
    {% include 'sidebar/bottom.html.twig' %}
{% endblock %}