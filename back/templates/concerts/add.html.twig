{% extends 'base.html.twig' %}
{% block title %}Ajout Concert - Back-office{% endblock %}
{% block body %}
    {{ include('sidebar/top.html.twig', {listItem: 8}) }}
    <style>

    </style>
    {{ include('page_title.html.twig', {title: "Ajout d'un concert"}) }}
    <div class="main px-5 mt-5">
        {{ form_start(concertAddForm) }}
        {{ form_row(concertAddForm.stage) }}
        {{ form_row(concertAddForm.name) }}
        {{ form_row(concertAddForm.description) }}
        {{ form_row(concertAddForm.genre) }}
        {{ form_row(concertAddForm.artist) }}
        {{ form_row(concertAddForm.date) }}
        {{ form_row(concertAddForm.duration) }}
        <button type="submit" class="btn btn-success float-right">Valider</button>
        {{ form_end(concertAddForm) }}
    </div>
    {% include 'sidebar/bottom.html.twig' %}
{% endblock %}