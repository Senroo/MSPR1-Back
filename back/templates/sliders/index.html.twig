{% extends 'base.html.twig' %}
{% block title %}Carousels - Back-office{% endblock %}
{% block body %}
    {{ include('sidebar/top.html.twig', {listItem: 8}) }}
    <style>

    </style>
    {{ include('page_title.html.twig', {title: 'Carousels'}) }}
    <div class="main">
        {% for sliders_img in sliders_imgs %}
            <div class="my-1">
                <span>{{ sliders_img.sliderId }}</span>
                <span>{{ sliders_img.position }}</span>
                <span>{{ sliders_img.picture }}</span>
            </div>
        {% endfor %}
    </div>
    {% include 'sidebar/bottom.html.twig' %}
{% endblock %}
