{% extends 'base.html.twig' %}
{% block title %}FAQ - Back-office{% endblock %}
{% block body %}
    {{ include('sidebar/top.html.twig', {listItem: 4}) }}
    <style>

    </style>
    {{ include('page_title.html.twig', {title: 'FAQ'}) }}
    <div class="main">
        {% for question in faq %}
            <div class="my-1">
                <span>{{ question.question }}</span>
                <span>{{ question.category }}</span>
                <span>{{ question.answer }}</span>
            </div>
        {% endfor %}
    </div>
    {% include 'sidebar/bottom.html.twig' %}
{% endblock %}
