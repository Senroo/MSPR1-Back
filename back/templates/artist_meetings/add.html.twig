{% extends 'base.html.twig' %}
{% block title %}Ajout Rencontre Artistes - Back-office{% endblock %}
{% block body %}
    {{ include('sidebar/top.html.twig', {listItem: 8}) }}
    <style>

    </style>
    {{ include('page_title.html.twig', {title: "Ajout d'une rencontre artiste"}) }}
    <div class="main px-5 mt-5">
        {{ form_start(artistMeetingAddForm) }}
        {{ form_row(artistMeetingAddForm.artist) }}
        {{ form_row(artistMeetingAddForm.location) }}
        {{ form_row(artistMeetingAddForm.date) }}
        <button type="submit" class="btn btn-success float-right">Valider</button>
        {{ form_end(artistMeetingAddForm) }}
    </div>
    {% include 'sidebar/bottom.html.twig' %}
{% endblock %}