{% extends 'base.html.twig' %}
{% block title %}Marqueurs Carte - Back-office{% endblock %}
{% block body %}
    {{ include('sidebar/top.html.twig', {listItem: 5}) }}
    <style>

    </style>
    {{ include('page_title.html.twig', {title: 'Marqueurs Carte'}) }}
    <div class="main">
        {% for marker in markers %}
            <div class="my-1">
                <span>{{ marker.type }}</span>
                <span>{{ marker.name }}</span>
                <span>{{ marker.description }}</span>
                <span>{{ marker.location }}</span>
            </div>
        {% endfor %}
    </div>
    {% include 'sidebar/bottom.html.twig' %}
{% endblock %}
